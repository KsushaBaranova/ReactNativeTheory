import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  viewDropdownStyle: {
    zIndex: 1000,
    width: '30%',
  },
  containerStyle: {
    height: 35,
  },
});

export default styles;
