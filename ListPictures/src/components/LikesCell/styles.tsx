import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 14,
  },
  imageStyle: {
    height: 20,
    width: 20,
  },
  viewContainerStyle: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default styles;
