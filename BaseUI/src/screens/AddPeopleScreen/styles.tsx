import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  backgroundStyle: {
    height: '100%',
  },
  flatListStyle: {
    flex: 1,
    width: '100%',
  },
});

export default styles;
